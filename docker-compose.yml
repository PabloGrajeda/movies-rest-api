version: '3.7'

services:

  app:
    container_name: Guaflix-backend
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - '.:/app'
      - '/app/node_modules'
    ports:
      - 5000:3000
    environment:
      - CHOKIDAR_USEPOLLING=true